/*
const sandwichOrder = (top1, top2, top3, top4, top5)=>{
    console.log(`We are preparing your sandwich order:
    ${top1}, ${top2}, ${top3}, ${top4}, ${top5}`);
   
   }
   sandwichOrder("ham","cheese","turkey","bacon","tuna");

   
for( i = 0 ; i < 10 ; i++ ){
 console.log(i);
};
for (i = 0; i < 6; i++ ){
 console.log(Math.random() * 49 + 1);
}


const sandwichOrder = (top1, top2, top3, top4, top5)=>{
 console.log(`We are preparing your sandwich order:
 ${top1}, ${top2}, ${top3}, ${top4}, ${top5}`);

}
sandwichOrder("ham","cheese","turkey","bacon","tuna")


let favouriteChoco = [
    "Mars",
    "Snickers",
    "Dairy Milk",
    "Picnic",
    "cococut"

   ];

   for (chocoIndex = 0; chocoIndex < favouriteChoco.length;
   chocoIndex++) {
    console.log(favouriteChoco[chocoIndex]);
   }
   


let cards = ["Diamond", "Spade", "Heart", "Club"];
let currentCard = "Heart";

while(currentCard != "Spade"){
    console.log(currentCard);
    currentCard = cards[Math.floor(Math.random()*4)];
}

console.log(currentCard);
*/
let favouriteFilms = [
    "Toys story",
    " Hit Man",
    "Silence Withness",
    "Diy Hard",
    "Commando",
    "Miserables"
   ];

   favouriteFilms.unshift("Bad Boy", "Js")

   for (filmIndex = 0; filmIndex < favouriteFilms.length;
   filmIndex++) {
    console.log(favouriteFilms[filmIndex]);
   }
   const filmcheck = () => {
    if (favouriteFilms[2] =="Gostbusters")
    console.log("Yas Ghostbusters")
    else(console.log("Booo, We Want Ghostbusters"))
   }
   filmcheck()




